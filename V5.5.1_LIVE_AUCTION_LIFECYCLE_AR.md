# ترقية V5.5.1 — دورة حياة المزاد المباشر

## الملفات المطلوب استبدالها

1. استبدل ملف `server.py` في المجلد الرئيسي للمشروع.
2. داخل مجلد `admin` استبدل الملفات: `index.html` و`app.js` و`styles.css`.
3. أعد تشغيل الخادم ثم حدّث صفحة الإدارة تحديثًا كاملاً.

> لا تحذف ملفات البيانات، ولا تستبدل مجلدات JSON أو `uploads`.

## السلوك المعتمد

- بيع قطعة في المزاد المباشر ينشئ طلب مبيعات آليًا باسم آخر مزايد معتمد.
- تظهر نتيجة القطعة المباعة في أرشيف جلسة البث: الفائز، السعر ورقم الطلب.
- لا يظهر السجل التقني للقطعة المباعة ضمن سجل المقتنيات النشط أو المستودع.
- القطعة التي تنتهي دون بيع تظهر في أرشيف جلسة البث فقط، ولا ينشأ لها طلب أو فاتورة.
- قطعة الكاميرا التي لا تملك صورة تظهر ببطاقة بديلة توضح أنها عُرضت مباشرة بالكاميرا.
- تعالج الترقية سجلات مبيعات الكاميرا القديمة وتخفيها من سجل المقتنيات مع إبقاء الطلبات والفواتير سليمة.

## التحقق بعد التركيب

- افتح: الإدارة ← البث المباشر ← أرشيف جلسات البث ← نتائج القطع.
- تحقق أن القطعة المباعة موجودة كذلك في: الطلبات والشحن.
- تحقق أن القطعة المباعة لا تظهر في: سجل المقتنيات أو رصيد المستودع.
- تحقق أن القطعة غير المباعة لا تظهر في الطلبات أو المالية.

الإصدار: `5.5.1-LIVE-AUCTION-LIFECYCLE`
