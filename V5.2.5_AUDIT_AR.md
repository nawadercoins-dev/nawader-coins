# نوادر العملات — V5.2.5

ترقية توحيد قنوات العرض والإدارة قبل الإطلاق.

## ما تم تنفيذه
- إضافة قسم إداري مستقل **العروض السريعة والمميزة** لإدارة التمييز على المقتنى الأصلي دون إنشاء نسخة جديدة.
- دعم: مميز، صفقة سريعة، مخفض، نسبة الخصم، شارة مخصصة، أولوية، وتاريخ انتهاء.
- الصفحة الرئيسية تعرض التمييز النشط فقط، وتحترم انتهاء الحملة والأولوية.
- إضافة زر واضح **إضافة مقتنى للسوق** وزر **إضافة مقتنى للمزاد** مع فتح نموذج المقتنى الموحد وتحديد القناة المطلوبة.
- إضافة **تصنيف السوق**: أوراق نقدية، عملات معدنية، فانتازيا، مميزة/نادرة، انتقالية، أطقم، أخرى.
- إضافة قسم إداري مستقل **البث المباشر 🔴** يعتمد على مقتنيات المستودع نفسها بالمعرّفات، بلا نسخ للسجلات.
- إنشاء جلسة بث، موعد بداية، ترتيب/اختيار المقتنيات، زيادة مزايدة، فتح مقتنى، إغلاقه بالبيع أو دون بيع، وإنهاء الجلسة.
- إضافة واجهة زوار `/live-auction` تظهر فقط عبر الصفحة الرئيسية عندما توجد جلسة مجدولة أو مباشرة.
- المزايدة المباشرة تتطلب حساب عميل موثق بالكامل، وتحدث السعر وآخر مزايد لحظيًا.
- عند اعتماد بيع المقتنى المباشر لآخر مزايد ينشأ طلب مرتبط بدورة الطلبات والشحن، مع إشعار للفائز.
- بيانات جلسات البث تحفظ في `DATA_DIR/live_auctions.json` وقت التشغيل، ولا يتم شحن ملف JSON ضمن الترقية.
- أضيفت جلسات البث إلى النسخة الاحتياطية الكاملة للنظام.

## سلامة البيانات
- لم تُضف أي ملفات JSON تشغيلية إلى حزمة الترقية.
- لا يوجد أي مسار `/var/data` داخل الحزمة.
- العروض المميزة والبث المباشر يحتفظان بمرجع `itemId` للمقتنى الأصلي بدل إنشاء مقتنى مكرر.

## فحوص ما قبل الإصدار
- `python -m py_compile server.py` ناجح.
- `node --check admin/app.js` ناجح.
- `node --check public/live_auction.js` ناجح.
- اختبار تشغيل الخادم على DATA_DIR مؤقت نجح.
- `/api/version` أعاد V5.2.5.
- `/api/public/live-auctions` عمل على بيانات تشغيل فارغة دون إنشاء ملفات داخل حزمة الكود.
- `/live-auction` أعاد HTTP 200.
