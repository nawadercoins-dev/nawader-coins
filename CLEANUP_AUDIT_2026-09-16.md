# تقرير بدء التنظيف الآمن — 2026-09-16

## القاعدة
لا حذف تلقائي ولا حذف بيانات تشغيلية. هذه قائمة تدقيق أولية فقط؛ أي عنصر لا يُحذف حتى يثبت عدم استخدامه وتوجد خطة رجوع.

## مرشحون واضحون للمراجعة
- `server(4).py` مقابل `server.py` — نسخة بديلة كبيرة داخل جذر المشروع، تحتاج إثبات هل تدخل في أي تشغيل/ترقية قبل عزلها.
- `public/public_home(2).html` مقابل `public/public_home.html` — اسم يدل على نسخة مكررة تحتاج تحقق مراجعها.
- `requirements.txt.txt` مقابل `requirements.txt` — ملف امتداد مكرر يحتاج التحقق من الاستخدام.
- مجلد `market_image_fix/` — مجلد إصلاح مرحلي منفصل يحتاج التحقق هل ما زال مستخدمًا أم أصبح أثرًا قديمًا.
- وجود نسخ متعددة من ملفات الواجهة في الجذر و`admin/` و`public/` مثل `public_market.html` و`public_auction.html` و`special_numbers.html` و`transitional_issues.html`؛ يجب تحديد المسار التشغيلي الفعلي قبل أي تنظيف.
- كثرة تقارير الإصدارات القديمة في الجذر لا تسبب وحدها بطء وقت التشغيل، لكنها مرشحة للنقل لاحقًا إلى مجلد توثيق/أرشيف بدل بقائها مع ملفات التشغيل.

## ملفات ثقيلة تحتاج مراقبة وليس حذفًا
- `server.py` كبير ويجمع وظائف كثيرة.
- `admin/app.js` كبير ويحتوي منطق أغلب واجهة الإدارة.
- `admin/styles.css` و`styles.css` كبيران نسبيًا.

الهدف هنا ليس حذف هذه الملفات، بل تقليل التكرار وتحميل/تنفيذ ما لا يلزم.

## أولويات التنظيف الحالية
1. إثبات الملفات الفعلية التي يقدمها الخادم للمستخدم والإدارة.
2. حصر النسخ البديلة وغير المرجعية.
3. عدم حذف أي نسخة قبل مقارنة المحتوى والتأكد من عدم وجود سكربت/Workflow يعتمد عليها.
4. مراجعة الأصول والصور من حيث التكرار والحجم، مع عدم المساس بصور المقتنيات الحقيقية.
5. بعد الإثبات فقط: عزل الملفات غير المستخدمة إلى أرشيف إصدار أو حذفها في فرع التنظيف مع اختبار رجوع.

## نتيجة اليوم حتى الآن
تم بدء الجرد وتحديد المرشحين. لم يتم حذف أي ملف أو بيانات، حفاظًا على النسخة المستقرة.
