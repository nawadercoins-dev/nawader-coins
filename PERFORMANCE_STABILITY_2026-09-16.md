# تقرير بدء صيانة الأداء — 2026-09-16

## الحالة
بدأت دورة الاستقرار P0 بعد بلاغ أن التنقل بين المالية والمستودع والسجلات والسوق يتباطأ أو يتوقف.

## ملاحظات مؤكدة من الكود
1. `admin/app.js` يستدعي `refresh(true)` عند الانتقال بين عدة أقسام إدارية.
2. `refresh()` يستدعي `all()`، و`all()` يجلب `/api/items` من الخادم، ما يعني إعادة جلب كامل بيانات المقتنيات في انتقالات متكررة.
3. حجم `admin/app.js` كبير، لذلك إعادة الحساب والرسم المتكرر لكل انتقال يمكن أن يضاعف الحمل على الجوال.
4. في الخادم، endpoint السوق يبني كل العناصر داخل `with LOCK`، لذلك أي معالجة بطيئة أثناء بناء بطاقات السوق تستطيع حجز القفل العام ومنع طلبات أخرى من التقدم مؤقتًا.
5. `public_market_item()` يستدعي `public_seller_identity()` لكل مقتنى، و`public_seller_identity()` يقرأ بيانات المشاركين؛ هذا نمط يحتاج قياسًا لأنه قد يكرر القراءة مع كل بطاقة.

## الإصلاح الأول المجهز
تمت إضافة أداة `tools/patch_admin_navigation_performance_v1.py` لتطبيق تحسين محافظ على واجهة الإدارة:
- كاش قصير المدى (12 ثانية) لبيانات `/api/items` أثناء التنقل فقط.
- تجميع الطلبات المتزامنة لنفس البيانات في طلب واحد.
- إبطال الكاش فور الحفظ أو الحذف.
- زر التحديث الصريح والحفظ يظلان قادرين على طلب بيانات جديدة.
- `refresh(force)` يمرر خيار force إلى `all(force)`.

## ما لم يُنفذ بعد
- لم يتم دمج أي تعديل في `main`.
- لم يتم تطبيق الأداة على النسخة التشغيلية.
- لم يتم تخفيف نطاق `LOCK` في الخادم قبل اختبار مستقل؛ لأنه تغيير حساس يحتاج قياسًا أولًا.

## الاختبار المطلوب قبل الاعتماد
1. فحص صياغة JavaScript بـ `node --check admin/app.js` بعد تطبيق الأداة.
2. فتح: المالية ← المستودع ← السجلات ← السوق ← الطلبات بالتتابع على الجوال.
3. تكرار دورة التنقل 5 مرات والتأكد من عدم التعليق.
4. حفظ مقتنى ثم التأكد أن البيانات الجديدة تظهر فورًا بسبب إبطال الكاش.
5. زر تحديث يدوي يجب أن يجلب نسخة جديدة فورًا.
6. التأكد من عدم تأثر الصلاحيات أو الفصل بين نوادر العملات ونوادر المقتنيات.

## خطة الرجوع
التغيير محصور في `admin/app.js`. في حال فشل الاختبار، يرجع الالتزام الخاص بتحسين التنقل دون لمس بيانات التشغيل.
