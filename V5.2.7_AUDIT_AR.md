# نوادر العملات — V5.2.7

## الإصلاحات
- إصلاح حفظ جلسات المزاد المباشر: مسارات البث كانت تستدعي `self.readj()` بينما الدالة غير موجودة في الخادم، ما كان يقطع الطلب قبل إرسال JSON ويظهر في المتصفح كـ 502 Bad Gateway. أضيف قارئ JSON موحد متوافق مع المسارات الحالية.
- توحيد بطاقات فانتازيا في الإدارة مع بطاقات المزادات باستخدام نفس بنية `auction-item / auction-card-image / auction-card-body / auction-actions`، وبالتالي نفس أبعاد الصورة، الاستطفاف، وارتفاع البطاقات.
- تحديث cache-busting لملفات الإدارة إلى V5.2.7 لمنع استمرار المتصفح في تحميل `app.js?v=4.8.2-archive`.
- لا تحتوي الحزمة على JSON تشغيلية ولا `/var/data`.

## ملاحظة
المستودع والبيانات التشغيلية تبقى مصدر الحقيقة ولا تُستبدل عند تثبيت هذه الحزمة.
