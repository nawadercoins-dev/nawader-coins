# V5.4.2 — إصلاح ReferenceError في إدارة البث

## السبب
كانت دالة إدارة جلسات البث تستدعي `item_title(...)` دون تعريفها داخل `admin/app.js`، لذلك كان `renderLiveAuctions()` يتوقف عند تحميل قائمة المقتنيات ويظهر الخطأ:
`ReferenceError: item_title is not defined`.

## الإصلاح
- إضافة دالة `item_title()` آمنة لتكوين اسم المقتنى من `marketTitle` أو `title` أو `itemTitle` أو الدولة/الفئة/السنة.
- تحديث cache-busting لملف `admin/app.js` إلى `v=5.4.2` حتى لا يستمر Chrome في استخدام نسخة `v=5.3.0` القديمة.
- تحديث نسخة CSS وعنوان صفحة الإدارة إلى V5.4.2.
- لا تغيير على قاعدة البيانات أو الجلسات أو مفاتيح LiveKit.
