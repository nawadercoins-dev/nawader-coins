# نوادر العملات — V5.3.0 LIVE CAMERA / WebRTC

## ما تم تنفيذه
- تحويل قسم «البث المباشر» من مزاد لحظي فقط إلى بنية بث فيديو حقيقي من كاميرا المتصفح/الجوال عبر WebRTC باستخدام LiveKit.
- عدم تسجيل أو تخزين الفيديو. تُحفظ فقط بيانات الجلسة والقطعة والمزايدات والفائز والطلب والفاتورة.
- وضعان للجلسة:
  1. بث حر بالكاميرا: لا يحتاج اختيار مقتنى من المستودع مسبقًا.
  2. بث محضّر: اختيار مقتنيات موجودة أصلًا في المستودع.
- استوديو جديد `/live-studio` للمذيع: فتح الكاميرا والميكروفون، بدء/إنهاء البث، فتح قطعة أمام الكاميرا بسعر بداية وزيادة ومدة، بيع لآخر مزايد أو إغلاق دون بيع.
- واجهة `/live-auction` للمشاهدين: مشاهدة الفيديو الحي، عرض القطعة الحالية والسعر والمؤقت وآخر مزايد، وتقديم المزايدة من الحساب الموثق.
- في البث الحر لا يُنشأ سجل مقتنى مكرر قبل البيع. عند اعتماد البيع فقط ينشئ النظام سجل الأصل في المستودع ثم ينشئ الطلب تلقائيًا للفائز.
- صلاحية جديدة `liveBroadcast` للبائعين. الإدارة تمنحها من شاشة الصلاحيات، ولا يستطيع البائع بث جلسة ليست له.
- دعم تعدد الجلسات: كل جلسة لها غرفة فيديو مستقلة باسم مشتق من معرف الجلسة.
- السماح للكاميرا والميكروفون في Permissions-Policy لنطاق الموقع نفسه.
- إضافة حالة تجهيز الفيديو في الإدارة، وزر «فتح استوديو الكاميرا» لكل جلسة.
- إضافة استوديو البث إلى حساب العميل وبوابة البائع عند تفعيل الصلاحية.
- تسجيل وقت بدء/إنهاء جلسة البث في بيانات الجلسة لتسهيل متابعة مدة التشغيل.
- فانتازيا: تحويل الإدارة إلى شبكة بطاقات موحدة مع المزادات: 4 أعمدة على الكمبيوتر، 5 على الشاشات العريضة، 3/2/1 حسب عرض الشاشة، مع نفس ارتفاع ونسبة صور بطاقات المزاد.

## إعداد خدمة الفيديو على Render
يلزم إضافة المتغيرات التالية في Environment فقط، ولا توضع داخل الكود:
- `LIVEKIT_URL` — رابط مشروع LiveKit بصيغة `wss://...`
- `LIVEKIT_API_KEY`
- `LIVEKIT_API_SECRET`

بدون هذه المتغيرات تبقى جلسات ومزايدات النظام محفوظة وتعمل، لكن استوديو الكاميرا سيعرض تنبيه أن نقل الفيديو لم يتم ربطه بعد.

## الخصوصية والتكلفة
- لا يوجد تسجيل فيديو أو Egress أو تخزين فيديو في V5.3.0.
- المذيع فقط يملك صلاحية نشر camera/microphone داخل غرفة الجلسة.
- المشاهدون يحصلون على صلاحية subscribe فقط ولا يستطيعون نشر كاميراتهم.
- رموز LiveKit قصيرة العمر وتصدر من الخادم؛ API Secret لا يصل إلى المتصفح.

## الاختبارات المنفذة قبل التغليف
- `python -m py_compile server.py` ناجح.
- `node --check admin/app.js` ناجح.
- `node --check public/live_studio.js` ناجح.
- `node --check public/live_auction.js` ناجح.
- فحص JavaScript المضمن في `account.html` و`seller_portal.html` ناجح.
- تشغيل خادم فعلي على DATA_DIR اختباري منفصل.
- اختبار `/api/version` أعاد V5.3.0.
- اختبار حفظ جلسة كاميرا من الإدارة نجح.
- اختبار بدء الجلسة وفتح قطعة حرة بسعر بداية/زيادة/مدة نجح.
- اختبار `/api/public/live-auctions` أظهر القطعة الحية والسعر والموعد.
- اختبار إصدار Publisher JWT أكد `canPublish=true` مع `camera` و`microphone` فقط.
- اختبار Viewer JWT أكد `canPublish=false` و`canSubscribe=true`.

## سلامة البيانات
هذه الحزمة CODE-ONLY ولا تحتوي ملفات JSON تشغيلية ولا `/var/data`، لذلك لا تستبدل بيانات العملاء أو المخزون أو الطلبات أو المزادات الحالية.
