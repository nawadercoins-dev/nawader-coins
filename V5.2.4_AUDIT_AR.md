# نوادر العملات — V5.2.4 مراجعة الاستقرار

هذا إصدار CODE ONLY مبني على V5.2.3-R2.

## الإصلاحات
- إصلاح فانتازيا في الإدارة: كانت الواجهة تعتمد على `state.items` غير المعرّف، بينما المصدر الصحيح هو `/api/items`. أصبحت فانتازيا تستخدم نفس مصدر الإدارة المشترك وتظهر مقتنياتها فورًا.
- إضافة فانتازيا لمسار التحديث عند فتح القسم من شريط الإدارة أو من لوحة التحكم.
- إزالة استدعاء فانتازيا العشوائي الذي كان يحدث بعد فتح أي قسم بسبب غياب الأقواس/الفصل في شرط الإصدارات الانتقالية.
- إضافة حقول فانتازيا/الأرقام المميزة/الإصدارات الانتقالية إلى بصمة البيانات المستخدمة لاكتشاف التغييرات.
- توحيد أزرار تحديث الإدارة مع حالة: جارٍ التحديث → تم التحديث / رسالة خطأ واضحة.
- الأزرار المغطاة: المستودع، الأرقام المميزة، فانتازيا، الإصدارات الانتقالية، المزادات المنتهية، الطلبات، المالية، مراقبة المقتنيات، المستحقات، سجل العمليات، الأرشيف، سلامة المنصة.
- تقوية صفحة المزادات العامة بإعادة المحاولة تلقائيًا حتى 3 مرات وعدم مسح المزادات المعروضة عند انقطاع لحظي بعد نجاح التحميل الأول.
- تقوية `/api/public/auctions` ضد فشل قراءة المصدر، مع إرجاع حالة قابلة لإعادة المحاولة بدل انهيار غير واضح.

## السلامة
- لا توجد ملفات JSON في الحزمة.
- لا توجد ملفات `/var/data`.
- لا يوجد أي حذف أو ترحيل لبيانات التشغيل.
- نجح `python -m py_compile server.py`.
- نجح `node --check admin/app.js`.
- نجح `node --check public/public_auction.js`.

## الملفات المعدلة فقط
- `server.py`
- `admin/app.js`
- `public/public_auction.js`

## اختبار ما بعد التثبيت
1. الإدارة → فانتازيا: يجب ظهور نفس مقتنيات فانتازيا الظاهرة للعامة.
2. اضغط تحديث فانتازيا: يظهر "جارٍ التحديث" ثم "تم التحديث".
3. جرّب تحديث المستودع والأرقام المميزة والإصدارات الانتقالية والطلبات.
4. افتح `/auction` بحساب موثق وتأكد من تحميل المزادات واستمرارها عند انقطاع لحظي.
