# نوادر العملات — V5.3.1 إصلاح اختيار جلسة استوديو البث

## المشكلة
عند فتح **استوديو الكاميرا** من صفحة الإدارة، إذا كان المتصفح يحمل أيضًا جلسة دخول عميل/مشارك لا تملك صلاحية `liveBroadcast`، كانت V5.3.0 تفحص المشارك أولًا ثم تتوقف، فتظهر رسالة أن صلاحية البث غير مفعلة وتبقى **جلسة البث فارغة** رغم وجود جلسات `live` و`scheduled` في الإدارة.

## الإصلاح
- جعل استوديو البث يفحص جلسة **الإدارة أولًا** ثم ينتقل إلى صلاحيات البائع فقط إذا لم تكن هناك جلسة إدارة صالحة.
- حماية `/api/live-auctions/admin` بحيث لا يعيد بيانات الجلسات إلا بعد تسجيل دخول الإدارة.
- الإبقاء على تمرير `?session=<id>` من زر **فتح استوديو الكاميرا** واختيار الجلسة المطابقة تلقائيًا.
- إضافة رسالة واضحة إذا كان معرّف الجلسة المطلوب غير موجود ضمن الجلسات المتاحة.
- تحديث cache-buster لملف الاستوديو إلى `5.3.1`.

## لا تغييرات
لم تُغيّر بنية البيانات أو المزادات أو الطلبات أو الفواتير أو إعدادات LiveKit.
