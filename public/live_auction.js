const $=id=>document.getElementById(id);const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));let sessions=[],activeId='',room=null,poll=null,tick=null;
async function j(url,opt={}){const r=await fetch(url,{cache:'no-store',credentials:'same-origin',headers:{'Content-Type':'application/json',...(opt.headers||{})},...opt});let d={};try{d=await r.json()}catch{}if(!r.ok){let e=new Error(d.error||`HTTP ${r.status}`);e.status=r.status;throw e}return d}
function renderList(){const rows=sessions.filter(s=>['scheduled','live'].includes(s.status));$('liveList').innerHTML=rows.map(s=>`<article class="live-session-card"><h3>${s.status==='live'?'🔴 مباشر الآن':'🗓️ مجدول'} — ${esc(s.title||'بث مباشر')}</h3><p>${esc(s.description||'')}</p><p>${esc(s.broadcasterName||'نوادر العملات')}</p><p>${esc(s.startAt||'')}</p><button class="watch-btn" onclick="watchSession('${esc(s.id)}')">${s.status==='live'?'▶ مشاهدة البث':'فتح الجلسة'}</button></article>`).join('')||'<p>لا توجد جلسات بث مباشر مجدولة حاليًا.</p>'}
async function load(){try{const d=await j('/api/public/live-auctions');sessions=d.sessions||[];renderList();if(activeId)renderActive()}catch{$('liveList').innerHTML='<p>تعذر تحميل جلسات البث المباشر.</p>'}}
function active(){return sessions.find(s=>String(s.id)===String(activeId))}
async function watchSession(id){activeId=id;const s=active();if(!s)return;$('viewerPanel').hidden=false;$('viewerTitle').textContent=(s.status==='live'?'🔴 ':'')+(s.title||'بث مباشر');$('viewerPanel').scrollIntoView({behavior:'smooth'});await connectVideo();renderActive();clearInterval(poll);poll=setInterval(load,1800);clearInterval(tick);tick=setInterval(renderCountdown,500)}
async function connectVideo(){if(room){await room.disconnect();room=null}const stage=$('videoStage');stage.innerHTML='<span style="color:white">جارٍ الاتصال بالبث…</span>';$('videoMsg').textContent='';try{if(!window.LivekitClient)throw Error('تعذر تحميل مكتبة الفيديو');const t=await j('/api/live-media/token?sessionId='+encodeURIComponent(activeId)+'&role=viewer');room=new LivekitClient.Room({adaptiveStream:true});room.on(LivekitClient.RoomEvent.TrackSubscribed,(track)=>{if(track.kind==='video'||track.kind==='audio'){const el=track.attach();el.autoplay=true;el.playsInline=true;if(track.kind==='video'){stage.querySelectorAll('video').forEach(x=>x.remove());stage.appendChild(el)}else document.body.appendChild(el)}}).on(LivekitClient.RoomEvent.TrackUnsubscribed,(track)=>track.detach()).on(LivekitClient.RoomEvent.Disconnected,()=>{$('videoMsg').textContent='انقطع اتصال الفيديو. يمكنك إعادة فتح الجلسة.'});await room.connect(t.url,t.token);stage.innerHTML='<span style="color:white">تم الاتصال. بانتظار كاميرا المذيع…</span>';$('videoMsg').textContent='الفيديو مباشر وغير مسجل.'}catch(e){stage.innerHTML='<span style="color:white">الفيديو غير متاح حاليًا</span>';$('videoMsg').textContent=e.message||'تعذر الاتصال بالفيديو'}}
function renderActive(){const s=active();if(!s)return;$('viewerTitle').textContent=(s.status==='live'?'🔴 ':'')+(s.title||'بث مباشر');const lot=s.currentLot,current=(s.items||[]).find(i=>String(i.id)===String(s.currentItemId||''));const title=lot?.title||current?.title||'';if(!title){$('bidPanel').innerHTML='<h3>بانتظار فتح القطعة التالية للمزايدة…</h3><p class="muted">استمر في مشاهدة الكاميرا.</p>';return}const step=Number(s.bidStep||1),currentPrice=Number(s.currentPrice||0),minimum=currentPrice>0?currentPrice+step:step;$('bidPanel').innerHTML=`<h3>${esc(title)}</h3><div class="live-price">${currentPrice.toLocaleString('ar-SA')} ر.س</div><p>قيمة الزيادة: ${step.toLocaleString('ar-SA')} ر.س</p><p>آخر مزايد: ${esc(s.latestBidderName||'لا يوجد بعد')}</p><div class="live-count" id="liveCountdown"></div><div class="live-actions"><input id="liveBidAmount" type="number" min="${minimum}" step="${step}" value="${minimum}"><button onclick="submitBid()">زايد الآن</button></div><p id="bidMsg" class="muted">المزايدة تتطلب حسابًا موثقًا بالكامل.</p>`;renderCountdown()}
function renderCountdown(){const s=active(),el=$('liveCountdown');if(!s||!el)return;if(!s.lotEndsAt){el.textContent='المزاد مفتوح حتى يغلقه المذيع';return}const ms=new Date(s.lotEndsAt).getTime()-Date.now();if(ms<=0){el.textContent='انتهى وقت القطعة';return}const sec=Math.ceil(ms/1000);el.textContent=`متبقي ${Math.floor(sec/60)}:${String(sec%60).padStart(2,'0')}`}
async function submitBid(){const amount=Number($('liveBidAmount')?.value||0),msg=$('bidMsg');try{const d=await j('/api/live-auctions/bid',{method:'POST',body:JSON.stringify({id:activeId,amount})});msg.textContent='✓ تم تسجيل مزايدتك.';await load()}catch(e){if(e.status===401){location.href='/account?return=/live-auction';return}msg.textContent=e.message||'تعذر تسجيل المزايدة'}}
async function closeViewer(){activeId='';clearInterval(poll);clearInterval(tick);if(room){await room.disconnect();room=null}$('viewerPanel').hidden=true}window.watchSession=watchSession;window.submitBid=submitBid;$('closeViewer').onclick=closeViewer;load();setInterval(()=>{if(!activeId)load()},7000);
